# Backlog

Не Jira-доска — рабочий список того, что осталось до реального запуска. Обновлять по ходу работы (см. правило документирования в [README.md](README.md)).

## Блокирует реальный запуск (не демо)

- [ ] Создать реальный Vercel Postgres инстанс, применить `npm run db:migrate`, проверить `/api/bookings` и `/api/availability` против живой БД (сейчас код готов, но не протестирован end-to-end — см. [docs/architecture.md](docs/architecture.md#что-не-сделано-архитектурно-значимое)).
- [ ] Создать Telegram-бота (@BotFather), заполнить `TELEGRAM_BOT_TOKEN`/`TELEGRAM_CHAT_ID`, проверить реальную доставку уведомлений.
- [ ] Дописать overlap-проверку длительности услуги при бронировании (сейчас конфликт проверяется только по точному совпадению времени — см. [docs/architecture.md#известные-упрощения-v1](docs/architecture.md#известные-упрощения-v1)).
- [ ] Заменить demo-данные (`src/data/services.json`, `src/data/masters.json`, `src/data/site-config.json`) на реальные: адрес, телефон, часы работы, прайс, состав мастеров.
- [ ] Реальная фотография барбершопа и мастеров взамен текущих стоковых Unsplash-фото (см. [docs/image-credits.md](docs/image-credits.md)) — стоки убрать перед реальным запуском, это не настоящие сотрудники.
- [ ] Rate limiting / anti-spam на `POST /api/bookings`.

## Полировка (можно делать на демо-версии)

- [ ] Прогнать все 4 страницы через skill `web-design-guidelines` (доступность/best practices).
- [ ] Прогнать интерактив/анимации через `review-animations` и `emil-design-eng` — сейчас перенесены только hover/active-состояния, скопированные из статичных макетов, без отдельного прохода полировки.
- [ ] `find-animation-opportunities` — есть ли места, где стоит добавить движение (например: появление карточек, прогресс-бар визарда, подтверждение брони).
- [ ] Убрать/пересмотреть `getSlotsForDate` для граничных случаев (например, бронирование "сегодня" на время, которое уже прошло — сейчас не фильтруется).

## Инфраструктура

- [ ] Создать и связать проект в Vercel (`vercel link`), первый деплой (`deploy-to-vercel` / `vercel-cli-with-tokens`).
- [ ] Подключить Google Analytics.
- [ ] Первый push в `origin` (`git@github.com:ai36/sportscavebarbershop.com.git`) — ждёт явного разрешения.

## Вне рамок v1 (осознанно отложено)

- Мультитенантность / шаблонизация под других клиентов.
- Индивидуальное расписание по мастеру.
- Отмена/перенос брони.
- WhatsApp-уведомления (архитектура готова принять — `src/lib/notify/index.ts`).
- Дополнительные страницы (About, Contact, Gallery, FAQ, Privacy/Terms).
- Админ-панель для редактирования услуг/мастеров без деплоя.
